<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to "Edit this book", edit_book_path(@book) %> |
  <%= button_to "Delete this book", @book, method: :delete, data: { confirm: "Are you sure?" } %> |
  <%= link_to "Back to books", books_path %>
</div>
